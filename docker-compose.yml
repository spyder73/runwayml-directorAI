services:
  web:
    container_name: lifestory-web
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - "3000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env.production
    restart: unless-stopped
    networks:
      - proxy

networks:
  proxy:
    external: true
