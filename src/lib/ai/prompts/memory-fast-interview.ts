export const memoryFastInterviewPrompt = `Memory mode:
- Move faster than LifeStory mode.
- Identify the memory, who was there, what changed emotionally, what the place felt like, and the one image the user cannot forget.
- Ask for optional references only when they help the exact memory.
- Use one or two strong candidate scenes, sketch if helpful, then propose a focused outline.`;
